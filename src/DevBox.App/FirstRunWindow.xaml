<Window x:Class="DevBox.App.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DevBox · First Run" Height="700" Width="940" MinHeight="580" MinWidth="800"
        Background="#F4F6F9" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#E8ECF2"/>
        </Style>
        <Style x:Key="Primary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Set up DevBox" FontSize="30" FontWeight="SemiBold"/>
            <TextBlock Text="Choose the verified components you want DevBox to prepare. Installed components are detected automatically and skipped."
                       Foreground="#6B7280" TextWrapping="Wrap" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="1 · Check" FontWeight="SemiBold"/>
                    <TextBlock Text="Detect installed runtimes" Foreground="#6B7280" FontSize="12" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="12" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="2 · Select" FontWeight="SemiBold"/>
                    <TextBlock Text="Choose missing components" Foreground="#6B7280" FontSize="12" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="3 · Install" FontWeight="SemiBold"/>
                    <TextBlock Text="Verify and install packages" Foreground="#6B7280" FontSize="12" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,12">
            <TextBlock Text="{Binding Status}" Foreground="#1E40AF" TextWrapping="Wrap"/>
        </Border>

        <ListBox x:Name="ComponentsList"
                 Grid.Row="3"
                 ItemsSource="{Binding Items}"
                 SelectionMode="Extended"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="1.25*"/>
                                <ColumnDefinition Width="1.35*"/>
                                <ColumnDefinition Width="2.4*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Margin="0,0,12,0"
                                      VerticalAlignment="Center"
                                      IsEnabled="{Binding CanInstallAutomatically}"
                                      IsChecked="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}, Mode=TwoWay}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding Details}" Foreground="#6B7280" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="8,0"/>
                            <Button Grid.Column="4"
                                    Content="Install"
                                    Style="{StaticResource Primary}"
                                    IsEnabled="{Binding CanInstallAutomatically}"
                                    ToolTip="{Binding Status}"
                                    Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                    CommandParameter="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Select installable" Click="SelectInstallable_Click"/>
            <Button Grid.Column="1" Content="Clear selection" Click="ClearSelection_Click"/>
            <StackPanel Grid.Column="2" Margin="16,3,0,0">
                <TextBlock x:Name="ProgressText" Text="Select components to install." Foreground="#6B7280" FontSize="12"/>
                <ProgressBar x:Name="InstallProgress" Height="7" Minimum="0" Maximum="100" Value="0" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="5" Margin="0,14,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
                <Button Content="Install selected" Style="{StaticResource Primary}" Click="InstallSelected_Click"/>
            </StackPanel>
            <Button Content="Continue to DevBox" HorizontalAlignment="Right" Click="Continue_Click"/>
        </Grid>
    </Grid>
</Window>