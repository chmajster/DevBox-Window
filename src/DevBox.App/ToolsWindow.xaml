<Window x:Class="DevBox.App.ToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DevBox · Tools" Height="620" Width="940" MinHeight="480" MinWidth="760"
        Background="#F4F6F9" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Button"><Setter Property="Padding" Value="12,7"/><Setter Property="Margin" Value="4"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Background" Value="#E8ECF2"/></Style>
        <Style x:Key="Primary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="Background" Value="#2563EB"/><Setter Property="Foreground" Value="White"/></Style>
    </Window.Resources>
    <Grid Margin="26">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel><TextBlock Text="Developer Tools" FontSize="27" FontWeight="SemiBold"/><TextBlock Text="Composer, Node.js, npm, pnpm and project workflows." Foreground="#6B7280" Margin="0,4,0,0"/></StackPanel>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                <Button Content="Project Manager" Click="ProjectManager_Click"/>
                <Button Content="Refresh" Style="{StaticResource Primary}" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="7" Padding="12" Margin="0,0,0,14">
            <DockPanel><TextBlock Text="{Binding Status}" Foreground="#1E40AF"/><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="Install Composer" Command="{Binding InstallComposerCommand}"/><Button Content="Install Node LTS" Command="{Binding InstallNodeCommand}"/><Button Content="Install pnpm" Command="{Binding InstallPnpmCommand}"/></StackPanel></DockPanel>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Tools}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="7" Padding="14" Margin="0,0,0,9">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="1*"/><ColumnDefinition Width="1.3*"/><ColumnDefinition Width="2.4*"/></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"/><TextBlock Text="{Binding Status}" Foreground="#6B7280"/></StackPanel>
                                <StackPanel Grid.Column="1"><TextBlock Text="VERSION" FontSize="10" Foreground="#9CA3AF"/><TextBlock Text="{Binding Version}" TextWrapping="Wrap"/></StackPanel>
                                <StackPanel Grid.Column="2"><TextBlock Text="INSTALL METHOD" FontSize="10" Foreground="#9CA3AF"/><TextBlock Text="{Binding InstallMethod}" TextWrapping="Wrap"/></StackPanel>
                                <StackPanel Grid.Column="3"><TextBlock Text="PATH" FontSize="10" Foreground="#9CA3AF"/><TextBlock Text="{Binding ExecutablePath}" TextWrapping="Wrap" Foreground="#6B7280"/></StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
