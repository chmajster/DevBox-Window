name: Apply final bug sweep round 8

on:
  push:
    branches:
      - fix/final-bug-sweep-round8
    paths:
      - .github/final-bug-sweep-round8-trigger

permissions:
  contents: write

jobs:
  apply:
    runs-on: windows-2025
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/final-bug-sweep-round8
          fetch-depth: 0

      - name: Apply fixes and tests
        shell: pwsh
        run: python .github/scripts/fix-round8-stop-cancellation.py

      - name: Commit fixes
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src tests
          if (-not (git diff --cached --quiet)) {
            git commit -m "fix: honor cancellation before forced service kill"
            git push origin HEAD:fix/final-bug-sweep-round8
          }

      - name: Validate
        shell: pwsh
        run: |
          git diff --check
          dotnet build DevBox.sln --configuration Release
          dotnet test DevBox.sln --configuration Release --no-build
